<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.test1.mapper.MainMyPageMapper">

	<select id="selectMainMyPageInfo" parameterType="String" resultType="com.example.test1.model.MainMyPage">

	  SELECT 
	    M.USER_ID,
	    M.NICKNAME,
	    M.STATUS,
	    P.POINT_TOTAL
	  FROM USERS M
	  LEFT JOIN POINT P ON M.USER_ID = P.USER_ID
	  WHERE M.USER_ID = #{userId}
	</select>
</mapper>




